apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app3
  template:
    metadata:
      labels:
        app: app3
    spec:
      containers:
      - name: html
        image: pierrezemb/gostatic
        args: ["-port=80", "-path=/app"]
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html
          mountPath: /app
      volumes:
      - name: html
        configMap:
          name: app3-html
          items:
          - key: index.html
            path: index.html